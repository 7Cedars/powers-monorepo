import { test } from '@playwright/test';

test.fixme('shows "no wallet connected" state when no wallet is connected', async () => {});

test.fixme('shows connected wallet address, ENS name, and proposal/execution/vote stats', async () => {});

test.fixme('copy address button copies the connected wallet address to the clipboard', async () => {});
